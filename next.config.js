/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Configuración opcional para mejorar estabilidad en entornos de desarrollo */
  reactStrictMode: true,
};

module.exports = nextConfig;
